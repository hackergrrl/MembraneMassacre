#ifndef _UTILS_H_
#define _UTILS_H_

int random(int m);
#define minf(a,b)	((a) < (b) ? (a) : (b))
#define maxf(a,b)	((a) < (b) ? (b) : (a))
#define absf(a)		((a) < 0 ? -(a) : (a))
//int max(int a, int b);
//int min(int a, int b);

#endif
